// Logo URL constant
// During development, we use the /@fs/ prefix to serve files from the filesystem.
// In production, the logo should be placed in the public/ folder.
export const LOGO_URL = '/logo.png';
